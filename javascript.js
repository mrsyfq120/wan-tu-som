// declare function with arguments
function getComputerChoice(types) {
// set the initial value to change
 for (let i = 0; i < 1; i++) {
// declare variables that caused things to randomize
  const random = types[Math.floor(Math.random() * types.length)];
    return random;
 }
}

// set the name of the types
let types = ['Rock', 'Paper', 'Scissors'];